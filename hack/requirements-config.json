{
  "variants": [
    {
      "name": "cpu",
      "dockerfile": "Dockerfile.konflux",
      "root_packages": [
        "mlserver",
        "mlserver-lightgbm",
        "mlserver-onnx[cpu]",
        "mlserver-sklearn",
        "mlserver-xgboost"
      ]
    },
    {
      "name": "cuda",
      "dockerfile": "Dockerfile.konflux.cuda",
      "root_packages": [
        "mlserver",
        "mlserver-onnx[cuda]"
      ]
    }
  ]
}
