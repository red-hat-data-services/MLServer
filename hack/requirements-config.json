{
  "variants": [
    {
      "name": "cpu",
      "dockerfile": "Dockerfile.konflux",
      "root_packages": [
        "mlserver",
        "mlserver-lightgbm",
        "mlserver-onnx[cpu]",
        "mlserver-sklearn",
        "mlserver-xgboost"
      ]
    },
    {
      "name": "cuda",
      "dockerfile": "Dockerfile.cuda.konflux",
      "root_packages": [
        "mlserver",
        "mlserver-onnx[cuda]"
      ]
    }
  ]
}
